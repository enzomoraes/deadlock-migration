services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: deadlock
      POSTGRES_PASSWORD: deadlock
      POSTGRES_DB: deadlock_development
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
